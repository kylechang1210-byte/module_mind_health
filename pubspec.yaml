name: module_mind_health
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.3.0
  provider: ^6.0.0
  shared_preferences: ^2.2.2
  share_plus: 9.0.0
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  sqflite: ^2.3.0
  path: ^1.8.3
  sqflite: ^2.4.2
  uuid: ^4.5.2
  path_provider: ^2.1.5
  supabase_flutter: ^2.12.0
  intl: ^0.20.2
  fl_chart: ^1.1.1
  provider: ^6.1.5+1
  audioplayers: ^6.5.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # --- UPDATED: Uncommented to allow loading images ---
  assets:
    - assets/